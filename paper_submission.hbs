<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Question Paper Submission - Question Paper Tracker</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>

    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header {
            width: 100%;
            background-color: #4CAF50;
            color: white;
            padding: 1rem;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        header .logo {
            width: 50px;
            height: 50px;
            background-size: cover;
            margin-right: 1rem;
        }

        header h2 {
            margin: 0;
        }

        main {
            width: 100%;
            max-width: 600px;
            margin: 2rem auto;
            background-color: white;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 2rem;
            border-radius: 8px;
        }

        .submission-container {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .submission-container h3 {
            margin-bottom: 1.5rem;
            color: #333;
        }

        form {
            width: 100%;
            display: flex;
            flex-direction: column;
        }

        form input[type="text"],
        form input[type="file"],
        form input[type="date"] {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 1rem;
        }

        form label {
            margin-bottom: 0.5rem;
            color: #555;
        }

        form button {
            width: 100%;
            padding: 0.75rem;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        form button:hover {
            background-color: #45a049;
        }

    </style>
</head>
<body>
<header>
    <div class="logo">
    </div>
    <h2>Submit Question Paper</h2>
</header>
<main>
    <div class="submission-container">
        <h3>Question Paper Submission</h3>
        <form method="post" action="/save">
            <input type="text" placeholder=" Professor Name" required name="Prof_name">
            <input type="text" placeholder="Title" required name="title">
            <input type="text" placeholder="Subject" required name="subject">
            <input type="text" placeholder="Subject Code" required name="subCode">
            <input type="file" accept=".pdf,.doc,.docx" required name="questionPaper">
            <label>Date of Submission </label>
            <input type="date" required name="date">
            <br>
            <button style="margin-top: 1%" type="submit">Submit</button>
        </form>
    </div>
</main>
</body>
</html>